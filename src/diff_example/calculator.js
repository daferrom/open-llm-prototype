function sumar(a, b) {
    console.log(`Sumando ${a} + ${b}`);
    return a + b;
}

function restar(a, b) {
    console.log(`Restando ${a} - ${b}`);
    return a - b;
}

function divide(a, b) {
    console.log(`Dividing ${a} - ${b}`)
    return a / b
}